```r
library(httr)

url = "https://ees-public-api-mock.vercel.app/api/v1/data-sets/dataSetId/query"
<% if body_json %>
body = '<%= body_json %>'
<% end %>

response = httr::<%= http_method.upcase %>(url<% if body_json %>,
  body=body, encode='json',
  content_type("application/json"),
  add_headers("Accept"= "application/json")<% end %>)

```
