```py
import requests

url = "https://ees-public-api-mock.vercel.app/api/v1/data-sets/dataSetId/query"
<% if body_json %>
headers = {
  "Content-Type": "application/json",
}
data = '<%= body_json %>'
<% end %>

response = requests.<%= http_method.downcase %>(url<% if body_json %>, headers=headers, data=data<% end %>)

print(response.text)
```
